# NB Whisper API

## Setup

Clone the repository:

```bash
git clone https://github.com/dananvaro/nb-whisper-api.git
cd nb-whisper-api
```

Start the API:

```bash
docker compose up -d
```

Stop the API:

```bash
docker compose down
```
